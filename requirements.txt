# BearMamba-3 — Python dependencies
# Tested on Ubuntu 24.04 + CUDA 12.x + Python 3.10
# ============================================================

# --- Core ML stack ---
torch>=2.5.0,<2.7
numpy>=1.24,<2.0
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3

# --- Mamba-3 (install separately, see README §1) ---
# MAMBA_FORCE_BUILD=TRUE pip install \
#     git+https://github.com/state-spaces/mamba.git \
#     --no-build-isolation --no-cache-dir
triton>=3.5.0
causal-conv1d>=1.5.0

# --- MIMO mode (optional, sm_90+ only) ---
# tilelang==0.1.8

# --- Visualization ---
matplotlib>=3.7
seaborn>=0.13
umap-learn>=0.5

# --- Data IO ---
PyYAML>=6.0
h5py>=3.10
scipy>=1.10  # for .mat file IO (already listed)

# --- Statistics ---
statsmodels>=0.14

# --- Progress + logging ---
tqdm>=4.66
rich>=13.0
